## Review
- Correct: OpenSpec is added at the repository root, `openspec/config.yaml` exists, and Pi manifest entries correctly reference `.pi/skills` and `.pi/prompts` (`package.json:30-37`). `npm exec openspec -- doctor` recognizes this checkout as a healthy OpenSpec root; `npm test` passes (35 tests).
- Blocker: The generated workflows invoke bare `openspec` (for example `.pi/prompts/opsx-propose.md:34`, `.pi/skills/openspec-propose/SKILL.md:41`), but installation only places it in `node_modules/.bin`. `scripts/pi-home.sh:47-52` and `scripts/init-agent-home.sh:222-223` never add that directory to `PATH`. Verified: `command -v openspec` fails and `PATH` lacks `<repo>/node_modules/.bin`; only `npm exec openspec -- ...` works. Thus `/opsx-*` workflows cannot use the repository-installed CLI on a fresh direct-home setup.
- Note (high): The package allowlist omits both `.pi/` and `openspec/` (`package.json:13-24`) while the manifest references `.pi` (`package.json:30-37`). `npm pack --dry-run` confirms neither the generated workflows nor `openspec/config.yaml` is included. Any npm/package-based use is incomplete, and the dev dependency is not installed for consumers.
- Note: No OpenSpec installation or launch-path regression test was added; existing tests do not mention OpenSpec.